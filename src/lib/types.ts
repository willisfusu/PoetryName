export interface PoemEntry {
  author: string;
  dynasty: string;
  content: string;
  book: string;
  title: string;
}

export interface BookConfig {
  value: string;
  name: string;
}

export interface GeneratedName {
  name: string;
  sentence: string;
  content: string;
  title: string;
  author: string;
  book: string;
  dynasty: string;
}
